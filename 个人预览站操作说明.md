# 个人 GitHub 预览站操作说明

这个仓库是原课题组网站的安全预览副本，包含新增的测试服务展示页。它不会影响原来的 `fudanpower.github.io` 网站。

## 你需要先做什么

1. 登录你自己的 GitHub 账号。
2. 新建公开仓库：

   `fudanpower-testing-preview`

3. 不要勾选自动创建 README、license 或 `.gitignore`。

## 发布到你的账号

在 PowerShell 运行：

```powershell
cd D:\CodexProjects\fudanpower-testing-preview
.\publish-preview.ps1 -GitHubUsername 你的GitHub用户名
```

如果 GitHub 弹出登录窗口，用你的账号登录。

## 开启 GitHub Pages

进入你新建的仓库：

`Settings -> Pages`

设置：

- Source: `Deploy from a branch`
- Branch: `master`
- Folder: `/ (root)`

保存后等 1-3 分钟。

## 检查是否上线

运行：

```powershell
cd D:\CodexProjects\fudanpower-testing-preview
.\check-pages-status.ps1 -GitHubUsername 你的GitHub用户名
```

如果显示 HTTP 200，说明页面已经能打开。

预览地址格式：

`https://你的GitHub用户名.github.io/fudanpower-testing-preview/testing-services/`

## 怎么自己编辑内容

主要编辑这个文件：

`_data/testing_services.yml`

常用字段：

- `platform.contact_email`：预约邮箱。
- `platform.contact_name`：联系人。
- `platform.contact_unit`：单位名称。
- `platform.description`：页面首屏简介。
- `devices`：设备列表。
- `charging`：收费说明。
- `notice`：风险和合规提示。

改完提交后，GitHub Pages 会自动重新构建。

## 怎么替换设备照片

把照片放到：

`images/testing-services/`

例如：

- `images/testing-services/b1505a.jpg`
- `images/testing-services/b1500a.jpg`
- `images/testing-services/radiant-premier-ii.jpg`

然后在 `_data/testing_services.yml` 中把对应设备的 `image` 改成：

`/images/testing-services/b1505a.jpg`

## 怎么判断要不要合并到原网站

先在你的预览网址里检查：

- 页面是否符合老师和课题组的展示风格。
- 设备信息是否准确。
- 联系邮箱是否正确。
- 收费说明是否符合学院/学校口径。
- 图片是否已经替换为真实设备照片。

确认后，再把这个仓库里的改动发给原网站维护者，让对方合并到 `fudanpower/fudanpower.github.io`。
